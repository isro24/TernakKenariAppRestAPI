import 'package:flutter/material.dart';

class ProfileViewBuyer extends StatelessWidget {
  const ProfileViewBuyer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}